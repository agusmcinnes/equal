# Supabase Environment Variables
# These are automatically provided by Supabase when deploying Edge Functions

# Supabase Project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Service Role Key (has admin access, bypasses Row Level Security)
# NEVER expose this key in client-side code!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Note: When deploying via Supabase CLI, these variables are automatically
# injected into your Edge Function environment. You don't need to set them manually.
